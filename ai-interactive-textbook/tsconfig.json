{
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
    "baseUrl": ".",      // root of your project
    "paths": {
      "@site/*": ["./*"] // maps @site/* to the project root
    }a
  },
  "include": [
    "src",
    "docs",
    ".docusaurus",
    "sidebars.ts",       // explicitly include sidebars
    "docusaurus.config.ts" // include config if using TS
  ],
  "exclude": [
    "build",
    "node_modules"
  ]
}


