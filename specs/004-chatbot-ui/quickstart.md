# Quickstart: Floating Chatbot UI

1.  Navigate to the `book` directory.
2.  Run `npm install` to install dependencies.
3.  Run `npm start` to start the Docusaurus development server.
4.  Open a browser and navigate to `http://localhost:3000`.
5.  The floating chatbot button should be visible in the bottom-right corner of the screen.
